import { ImagesNameFilterPipe } from './images-name-filter.pipe';

describe('ImagesNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagesNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
